name: Deploy StreamForge

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/streamforge/infra/streamforge-infra || cd ~/streamforge-infra
            git pull
            ENVIRONMENT=prod docker compose -f docker/docker-compose.base.yml -f docker/docker-compose.prod.yml up -d --build
