# Mobile app does not load this file automatically yet.
# API hosts are configured in src/core/config/api.config.ts
#
# For a physical Android/iOS device, change DEV_MACHINE_HOST in api.config.ts
# to your computer's LAN IP (e.g. 192.168.1.10).
#
# Auth service configuration lives in:
#   services/auth-service/.env
